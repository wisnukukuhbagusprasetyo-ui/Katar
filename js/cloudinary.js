export async function uploadToCloudinary(file,folder='kartar'){const cloudName='YOUR_CLOUD_NAME'; const uploadPreset='YOUR_UPLOAD_PRESET'; const fd=new FormData(); fd.append('file',file); fd.append('upload_preset',uploadPreset); fd.append('folder',folder); const res=await fetch(`https://api.cloudinary.com/v1_1/${cloudName}/upload`,{method:'POST',body:fd}); if(!res.ok) throw new Error('Upload gagal ke Cloudinary'); const data=await res.json(); return data.secure_url;}