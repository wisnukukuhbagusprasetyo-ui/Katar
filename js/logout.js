import { auth } from './firebase.js'; import { signOut } from 'https://www.gstatic.com/firebasejs/10.14.1/firebase-auth.js'; export async function doLogout(){await signOut(auth); alert('Berhasil keluar!'); location.href='/auth/login.html';} document.addEventListener('DOMContentLoaded',()=>{const b=document.getElementById('btnLogout'); if(b) b.onclick=(e)=>{e.preventDefault(); if(confirm('Keluar dari akun?')) doLogout();};});